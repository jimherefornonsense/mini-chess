from setuptools import setup, find_packages

setup(
    name='mini_chess',
    version='0.1',
    description='A package for playing mini chess',
    author='jimherefornonsense',
    packages=find_packages(),
    install_requires=[
    ]
)
